localhost {
    encode gzip

    handle /api* {
        reverse_proxy localhost:3000
    }

    handle {
        root * /usr/share/caddy
        try_files {path} /index.html
        file_server
    }
}
